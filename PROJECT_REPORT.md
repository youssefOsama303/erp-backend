# 📊 Nexus ERP — الدليل الشامل والمواصفات الفنية

**تاريخ التحديث:** أبريل 2026

المشروع عبارة عن نظام لتخطيط موارد المؤسسات **(ERP - Enterprise Resource Planning)** مبني بتقنيات الويب الحديثة، ومصمم ليُحاكي الأنظمة العالمية الضخمة مثل **(SAP S/4HANA)** ولكن بواجهة مستخدم أكثر عصرية، مرونة، وسرعة في الاستجابة.

---

## 🎯 1. ما هو هذا المشروع؟ (الفكرة والهدف)

**Nexus ERP** هو نظام برمجي مركزي لإدارة كافة أقسام الشركة في مكان واحد. بدلاً من أن تستخدم الشركة برنامجاً منفصلاً للمحاسبة، وآخر للمخازن، وثالثاً للموارد البشرية، يجمع هذا النظام كل هذه الأقسام معاً.
تتواصل كل وحدة (Module) مع الأخرى بشكل لحظي. فمثلاً: عندما يتم شراء منتج عبر (المشتريات)، تزيد الكمية تلقائياً في (المخازن)، وتُخصم التكلفة من (المحاسبة المالية).

### 🛠 التقنيات المستخدمة (Tech Stack):
* **الواجهة الأمامية (Frontend):** 
  - (Vanilla JavaScript SPA) تطبيق صفحة واحدة فائق السرعة، يعتمد على الـ DOM Manipulation المباشر دون الحاجة لمكاتب ثقيلة مثل React لتسريع الأداء لأقصى حد.
  - تصميم عصري يدعم الـ Dark Mode.
* **الخادم (Backend):** 
  - Node.js و Express.js لمعالجة البيانات وتأمين الـ APIs (مسارات الربط).
* **قاعدة البيانات (Database):** 
  - PostgreSQL الجبارة، للتعامل مع البيانات الضخمة والـ Transactions المحاسبية المعقدة.

---

## 🏢 2. مَن هي الشركات والفئات المستهدفة؟

هذا النظام ليس مصمماً للبقالات أو المحلات الصغيرة، بل هو **نظام مؤسسي (Enterprise-Grade)** موجّه للفئات التالية:

1. **الشركات المتوسطة والكبيرة (Mid-Market to Enterprise):**
   الشركات التي يتجاوز عدد موظفيها 50 موظفاً وتتطلب أقساماً منسقة (محاسبة، مشتريات، مخازن، موارد بشرية).
2. **الشركات الصناعية (Manufacturing):**
   لوجود وحدة "تخطيط الإنتاج (PP)" التي تتعامل مع أوامر التصنيع، وقوائم المواد المكونة للمنتج (BOM)، مما يجعله مثالياً لمصانع التجميع والتغليف.
3. **شركات اللوجستيات والتوزيع (Supply Chain):**
   بفضل نظام المخازن المتقدم (EWM - Extended Warehouse Management) الذي يتيح تتبع البضائع على مستوى (الرف) والـ (Zone).
4. **الشركات متعددة الفروع أو الشركات القابضة:**
   نظراً لاعتماد النظام على خاصية **Multi-Tenancy** (قريباً)، حيث يمكن فصل بيانات فروع الشركة عن بعضها بنظام سحابي مرن.

---

## ⚙️ 3. الوحدات البرمجية المتوفرة (Modules)

يغطي النظام النطاقات الآتية:

1. **اللوحة الرئيسية (Dashboard):** عرض مؤشرات الأداء (KPIs) للإدارة العليا.
2. **المحاسبة المالية (FI/CO):** القيود اليومية، دفتر الأستاذ، متابعة الإيرادات والمصروفات.
3. **الموارد البشرية (HR/HCM):** حساب الرواتب (Payroll)، إدارة الموظفين، وتسجيل الإجازات.
4. **المخازن (EWM):** توزيع مساحات التخزين، أوامر صرف البضائع، ومتابعة الكميات.
5. **تخطيط الإنتاج (PP):** تتبع أوامر التصنيع وتحويل المواد الخام إلى منتجات نهائية.
6. **المبيعات والمشتريات (SD/MM):** إصدار أوامر الشراء (POs)، وتتبع الموردين والمبيعات.
7. **إدارة علاقات العملاء والمشاريع (CRM/Projects):** متابعة مستجدات العملاء المحتملين وتتبع مراحل إنجاز المشاريع الداخلية.
8. **الحوكمة والمخاطر (GRC):** (Governance, Risk, and Compliance) تسجيل مخاطر الشركة المؤسسية، والامتثال لقواعد أمن المعلومات، وفصل الصلاحيات.
9. **منصة (BTP - Business Technology Platform):** واجهة نظام داخلية للمسؤول المعماري لمشاهدة استهلاك الخادم والـ Logs فورياً.

---

## 🚀 4. طريقة الاستخدام والتشغيل محلياً (How to Use)

### أ. التحضير (Pre-requisites)
1. تأكد من تنصيب **Node.js** على جهازك.
2. تأكد من وجود وتفعيل خادم قاعدة بيانات **PostgreSQL** محلي أو سحابي.

### ب. إعداد النظام
1. قم بنسخ المشروع وملفاته.
2. افتح ملف `.env` وقم بضبط إعدادات الربط بقاعدة البيانات `DATABASE_URL` (مثل: `postgres://user:password@localhost:5432/nexus_erp`).
3. افتح الطرفية (Terminal) في مسار المشروع واكتب لتنصيب الحزم:
   ```bash
   npm install
   ```

### ج. تشغيل الخادم
لتشغيل الخادم، قم بكتابة:
```bash
npm run dev
```
سيبدأ الخادم بالعمل على البورت `5000` (بشكل افتراضي)، وسيقوم تلقائياً بفحص قاعدة البيانات وإنشاء الجداول الناقصة وبذر (Seed) بيانات أولية لتجربة النظام!

### د. الدخول للنظام (Login)
* افتح المتصفح على الرابط: `http://localhost:5000/admin/login.html`
* استخدم صلاحيات الإدارة:
   - **البريد:** `admin@nexus.com`
   - **كلمة السر:** `admin123`

---

## 🔒 5. الأمان والصلاحيات (RBAC)
النظام محمي بنظام للتحكم في الصلاحيات بناءً على الدور (Role-Based Access Control).
كل طلب يعبر نحو الخادم (Backend) يتم فحصه: هل الموظف (موارد بشرية)، (محاسب)، أم (مشاهد)؟ ويتم إخفاء أو إظهار القوائم في السايدبار بناءً على هذه الجاهزية.

---
*هذا التقرير الحي يوثّق المعمارية الحالية والموجهة حصرياً لأغراض التقييم التقني للجاهزية للإنتاج الحقيقي.*
